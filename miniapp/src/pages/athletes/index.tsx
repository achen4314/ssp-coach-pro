import { View, Text } from '@tarojs/components';

export default function Athletes() {
  return (
    <View style={{ padding: 20, background: '#0a0f0f', minHeight: '100vh' }}>
      <Text style={{ color: '#a0c040', fontSize: 24, fontWeight: 'bold' }}>运动员</Text>
    </View>
  );
}
